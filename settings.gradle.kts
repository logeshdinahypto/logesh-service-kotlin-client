rootProject.name = "logesh-service-kotlin-client"

include("logesh-service-models", "logesh-service-client")

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        google()
    }
}
